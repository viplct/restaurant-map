# ═══════════════════════════════════════════════════════════
# Docker Compose Environment Variables
# ═══════════════════════════════════════════════════════════
# This file is used by docker-compose.yml ONLY.
# Individual services (backend, frontend) have their own .env files.
# ═══════════════════════════════════════════════════════════

# ── MySQL Container ────────────────────────────────────────
DB_ROOT_PASSWORD=rootpassword
DB_DATABASE=restaurant_map
DB_USERNAME=restaurant
DB_PASSWORD=password

# ── Application Environment ────────────────────────────────
APP_ENV=local

# ── Frontend Containers ────────────────────────────────────
# Used by Next.js containers (frontend & frontend-admin)
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
